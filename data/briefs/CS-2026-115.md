# CS-2026-115: Seed first autonomous CRO hypothesis — subscription-first PDP presentation (T2 cycle 1)

**Department:** DTC-website
**Platforms:** [shopify, auto-optimize-loop]

## Metadata
- **ID:** CS-2026-115
- **Status:** approved
- **Subtype:** quick
- **Category:** experiment-seed
- **Owner:** Katie
- **Date Created:** 2026-04-24
- **Risk Level:** low (seeding only — deploy gated by Slack approval at awaiting_approval phase)
- **Review Tier:** express
- **Autonomy Tier:** tier_1 (first autonomous CRO loop cycle)
- **Execution Target Date:** 2026-04-24
- **Linked Calendar Activity:** null
- **Spawned From:** manual — Katie launching T2 validation cycle per `architecture/2026-04-24-t2-first-cro-experiment-scope.md`

## Source
- **Parent scope doc:** `architecture/2026-04-24-t2-first-cro-experiment-scope.md`
- **Build spec:** `architecture/build-spec-autonomous-optimization-architecture.md` (hypothesis #4 in pdp-atc backlog)
- **Surface config:** `config/surface-configs/pdp-atc-theme.yaml`
- **State file:** `drinkkenetik/kenetik-shopify-theme` → `auto-optimize/state.json`

## Pre-Step: Active ChangeSet Check
- Open shopify ChangeSets at creation: CS-2026-114 (PDP bug fix — auto-select first available pack size on flavor toggle). No contamination — bug fix is on `cw-01-product-banner.liquid` (Commander Wolf PDP banner), this experiment targets `snippets/buy-buttons.liquid` (global buy-box). Different files, different code paths.
- Contamination risk: None — state.json seed only. Variant rendering not active until Slack `approve` at awaiting_approval phase.
- Linked calendar activity: null

## Hypothesis
Flip the PDP buy-box default from one-time to subscription-first presentation (subscription option rendered above one-time, selected by default) to test whether anchoring to the higher-value option and simplifying the choice architecture lifts add-to-cart rate on pdp-atc.

**Element changing:** buy-box default state + option ordering in `snippets/buy-buttons.liquid` (to be written by orchestrator at step 2 of launch sequence).

**Data rationale:** Hypothesis #4 in the pdp-atc build-spec backlog. Subscription-first is a known LTV lever in CPG; its compound effect on ATC rate is untested on Kenetik's audience. This cycle measures whether the framing flip is net-neutral, accretive, or cannibalistic on ATC.

**Expected direction:** ATC rate up (+).

**Disproof:** ATC rate flat or down at ≥95% Bayesian confidence over the 7-day measurement window, OR subscription attach rate drops with no net ATC lift (pure cannibalization pattern — tells us subscription-first looks better on the surface but isn't a true decision-architecture win).

## Resolution Path

| Step | Considered? | Result |
|---|---|---|
| Diagnose | Yes | Scope doc identifies 5 candidate hypotheses from build-spec backlog. Katie selected subscription-first from 4 options presented. |
| Remove | N/A | Not applicable — seeding new experiment, not removing existing behavior. |
| Refine | **Yes — approved** | Seed `seeded_hypothesis` into `auto-optimize/state.json` so orchestrator uses this hypothesis instead of generating on its own at step 2 of the launch sequence. |
| Add | N/A | No new infra; infra already built per build-spec v1.1 completion checklist. |

Resolution Path: **Refine (state.json seed)** — +7 fields to `experiments.pdp-atc`, 0 removed.

## Change
Patch `drinkkenetik/kenetik-shopify-theme` → `auto-optimize/state.json`:

- Add `seeded_hypothesis` (string) — the hypothesis paragraph above, so orchestrator reads it at step 2 instead of generating.
- Add `seeded_by` = `"katie"` — attribution.
- Add `seeded_at` = `"2026-04-24T00:00:00Z"` — timestamp.
- Add `cycle_tag` = `"t2-cycle-1"` — ties state transitions to the T2 validation scope for retrospective.

State remains `phase: "idle"`. No variant yet. No A/B split yet. No traffic affected.

## Objective & Expected Impact

```yaml
expected_impact:
  revenue:
    upside: null  # T2 goal is infra validation, not revenue. Any lift is bonus.
    expected: "[VALIDATION RUN — no revenue target] Primary objective is observing all 6 phase transitions fire cleanly. Revenue outcome is the business signal, not the KPI for T2."
    downside: 0  # guardrails cap at -20% ATC / -20% CVR / +20% bounce on 7-day aggregate
  margin:
    upside: null
    expected: "Subscription attach rate may shift +/-. Margin effect depends on attach mix × 7-day sample."
    downside: 0
  primary_metric: add_to_cart_rate
  primary_metric_target: "Validation only — success = full idle→awaiting_approval→canary→measuring→analyzing→decided→idle cycle observed with all Slack notifications firing. Keep/discard decision on variant is business signal but does not determine T2 success."
  L1_margin: "neutral (infra seed)"
  L2_cash: "neutral"
  L3_customer_quality: "neutral — subscription-first may shift attach toward subscription which is higher LTV"
  L4_learning: "HIGH — first autonomous CRO cycle validates reference architecture that Klaviyo + paid media + ChangeSet auto-execute all copy. Any failure mode caught here saves 3 downstream implementations."
  L5_brand_check: "neutral — no copy, claims, or voice changes in seed; variant Liquid change will be claims-checked + brand-voice-checked at orchestrator step 2 pre-flight."
```

## Measurement Plan
- **Surface:** pdp-atc
- **Primary metric:** add_to_cart_rate (from GA4 `ao_pageview` + `add_to_cart` events, segmented by `ao_experiment` + `ao_variant` custom dimensions)
- **Measurement window:** 7 days at 50/50 after 24h canary at 10%
- **Min sample:** 100 sessions per arm (pdp-atc-theme.yaml)
- **Analysis:** `scripts/experiment_analyzer.py` — Beta-Binomial conjugate, 100k Monte Carlo samples, 95% decision threshold
- **Guardrails (7-day aggregate vs control):**
  - Tier 1 emergency revert: ATC drop -20%, CVR drop -20%, bounce rate +20%
  - Tier 2 pause notify: ATC drop -10%, bounce +10%, 3 consecutive discards
  - Tier 3 celebrate: cumulative lift across cycles > 15%

## Observation Plan (who watches, when)
| Phase | Duration | Who | Checking |
|---|---|---|---|
| awaiting_approval | 0–24h | Katie | Preview renders, variant matches hypothesis, mobile OK |
| canary (10%) | 24h | Katie (manual — cron is time-based, not metric-based per known TODO in cron.yml) | Bounce + ATC on GA4 realtime; no layout break |
| measuring (50%) | 7d | Katie (daily glance) | Guardrails within thresholds; sessions accumulating |
| analyzing | ~1 Cowork session | Katie + Devon | Bayesian output sane; guardrail summary clean |
| decided | ~1 Cowork session | Katie | Approves keep/discard; merge or reset fires |

## Success Criteria (T2)
Infrastructure validation — does the reference architecture work end-to-end?
- All 6 phase transitions fire cleanly
- All Slack notifications deliver (hypothesis, deploy, canary-start, measuring-start, analysis-complete, decision)
- Preview theme isolated from published theme (safety check passes)
- A/B cookie persists across >1 session for test visitor
- GA4 receives `ao_pageview` with correct `ao_experiment` + `ao_variant`
- Bayesian analyzer returns valid decision (keep/discard/inconclusive) with lift + CI
- Final merge-or-reset completes without stale branches or config entries
- state.json returns to clean `idle`

**Whether the subscription-first variant wins or loses is separate from T2 success.** We want to validate the loop regardless of business outcome on this particular hypothesis.

## Dependencies
- Cowork `auto-optimize-orchestrator` scheduled task must be runnable (open question #1 in scope doc — Devon to confirm)
- 4 GitHub secrets in theme repo: SHOPIFY_CLI_THEME_TOKEN, SHOPIFY_STORE, SLACK_WEBHOOK_URL, SLACK_BOT_TOKEN (open question #2 — Devon to confirm)
- GA4 custom dimensions `ao_experiment` + `ao_variant` registered (open question #3 — confirmed in build-spec v1.1 completion checklist, row marked `[x]`)
- Guardrail thresholds accepted as-is at -20% tier 1 (open question #4 — Katie confirmed 2026-04-24 in hypothesis-seed session)
- Hypothesis #1 selection (open question #5 — Katie selected subscription-first 2026-04-24)

## Approval Trail
- **Approved by:** Katie
- **Approved date:** 2026-04-24
- **Approval context:** Hypothesis-seed session with Claude. Hypothesis selection = subscription-first presentation (from 4 options). Guardrails = keep -20% tier-1 as-is (per pdp-atc-theme.yaml). Awaiting Devon sign-off on open questions 1–3 from scope doc before orchestrator fire.

## Next Action
Write this CS + tracker update + state.json seed to both repos on main. Fire orchestrator per Step 2 of scope doc once Devon confirms open questions 1–3.

## File
`cgo-logs/changesets/CS-2026-115.md`
